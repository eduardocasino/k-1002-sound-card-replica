MEMORY {
    ZP:     start = $0000, size = $100,  file = "07_0_%O";
    SYSRAM: start = $0200, size = $200,  file = "07_2_%O";
    EXPRAM: start = $2000, size = $1000, file = "08_%O";
}

SEGMENTS {
    ZEROPAGE: load = ZP,     type = rw;
    CODE:     load = SYSRAM, type = rw;
    CODE2:    load = EXPRAM, type = rw;
}
