MEMORY {
    ZP:     start = $0000, size = $100, file = "03_0_%O";
    STACK:  start = $0100, size = $100, file = "03_1_%O";
    SYSRAM: start = $0200, size = $200, file = "03_2_%O";
}

SEGMENTS {
    ZEROPAGE: load = ZP,     type = rw;
    PAGE1:    load = STACK,  type = rw;
    PAGE2:    load = SYSRAM, type = rw;
}
