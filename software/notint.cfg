MEMORY {
    ZP:     start = $0000, size = $100, file = "04_0_%O";
    SYSRAM: start = $0200, size = $200, file = "04_2_%O";
}

SEGMENTS {
    ZEROPAGE: load = ZP,     type = rw;
    CODE:     load = SYSRAM, type = rw;
}
